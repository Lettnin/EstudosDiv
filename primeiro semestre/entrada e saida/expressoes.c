#include <stdio.h>
#include <math.h>
//int = inteiro
//%d = uso quando uso int para numeros inteiro
//#include <math.h>, para operaçoes matematicas//
//%f = float
// M_PI = pi

//exercicios em aula dia 26/04

    // r = (a+b) / (c+d);

    // b = a/(pow(c,2+d));

    // c = pow(a,2)/(b-1);

    // d = a/(b+e) - e/(f*g);

    // e = (-b + sort(pow(b,2)- 4*ac))/2*a;

    // f =  ((a-b)/(c+d)) + ((d+b)/(a-b));

    // g = pow(r,3)*t+y;

    // h = (a+b)/ pow(c,2)+(d-g)/pow(e,2)+pow(f,2);

    
    
    